services:
  admin:
    image: transcode-admin:local
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      BACKEND_BASE_URL: "https://trapi.demeter-sante.fr/api/v1"
